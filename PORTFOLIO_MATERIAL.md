# breachchain — 포트폴리오 작성용 소스 자료

> 이 문서는 최종 포트폴리오 원고가 아니라, **포트폴리오를 작성할 때 참고할 사실 자료(raw material)**입니다. 여기 있는 내용을 바탕으로 이력서/포트폴리오 문서를 새로 작성해주세요. 과장 없이, 실제로 한 것과 안 한 것을 명확히 구분해서 써주시면 좋겠습니다.

- 저장소: https://github.com/DeanJun/breachchain
- 작업 기간: 2026-08-12 ~ 2026-08-15 (4일)
- 지원 직무: 통신사 보안 진단(모의침투테스트) 포지션

---

## 1. 한 줄 요약

시나리오 기반 침해 진단 자동화 도구. 실제 Atomic Red Team 데이터(231개 안전 필터링된 절차)를 MITRE ATT&CK 전술 순서로 실행하고, 상태 기반으로 조건을 거르며, 정찰→초기접근(브루트포싱)→진단(ART+KISA CIIP)→통합 리포트까지 한 번에 도는 파이프라인을 처음부터 직접 설계·구현했다.

## 2. 왜 만들었나 (동기)

지원 직무의 핵심 요구사항인 "MITRE ATT&CK 기반 시나리오 설계/진단 능력"과 "IoT 단말 보안 진단"을 증명할 기존 프로젝트가 없어서, 직접 만들어 실행 로그·리포트 산출물로 근거를 남기기로 했다. 검증 절차 자체(수백~수천 개)는 새로 만들지 않고, 공개된 Atomic Red Team의 절차 정의를 실행 라이브러리로 활용하는 대신, **오케스트레이션 레이어(로더, 실행기, 상태 관리, tactic 매핑, 리포트)를 전부 직접 설계·구현**했다. 이는 실무 진단 자동화 도구(MITRE Caldera 등)와 동일한 구조다.

## 3. 지원 직무 요구사항과의 대응 (JD 매핑)

| JD 요구사항 | 이 프로젝트에서 증명하는 것 | 비고 |
|---|---|---|
| MITRE ATT&CK 기반 시나리오 설계/진단 능력 | ART 1786개 절차 파싱 → 안전 필터(231개 통과) → STIX 데이터셋 기반 technique→tactic 매핑(697개 기법) → ATT&CK 공식 전술 순서(Reconnaissance→...→Impact)로 실행 → 상태 기반 게이팅 | **가장 강하게 증명되는 항목** |
| 진단 절차 자동화(반복 검증 로직 설계/구현) | 로더/실행기/상태관리/리포트 오케스트레이션을 전부 직접 구현. 실전에서 실제 버그(subprocess 무한대기, SFTP 경로 문제, `/proc` 재귀 스캔 멈춤, 잘못된 서브넷 기본값)를 발견하고 근본 원인까지 수정한 기록이 커밋 히스토리에 남아있음 | |
| IoT 단말 보안 진단 | OpenWrt(BusyBox 기반) VM을 IoT 모뎀 대리로 구축, 실제 도구를 돌려 "ART가 GNU/Linux 환경을 전제하고 있어 임베디드 대상엔 상당수 안 맞는다"는 걸 실측으로 확인·문서화. `device_fingerprint.py`로 OS/커널/보드모델 기반 임베디드 여부 자동 판정 구현 | 펌웨어/프로토콜 레벨 분석은 미포함 (5절 한계 참고) |
| 최신 위협 동향 분석 및 대응방안 도출 | 국내 표준(KISA 주요정보통신기반시설 기술적 취약점 분석·평가 가이드) 진단 스크립트를 실제 원격 대상에 실행 가능하게 통합(SFTP 업로드+SSH 원격실행+결과 파싱), 리포트에 취약 항목별 조치방법까지 표시 | |
| 웹/API/시스템/네트워크 진단 기법 | 포트 스캔(`recon.py`), 웹 경로 브루트포싱(`web_recon.py`, catch-all 오탐 필터링 로직 포함), SSH 브루트포싱(`bruteforce.py`), CVE 버전 매칭(`vuln_scan.py`) | 시스템·네트워크는 강함, 웹/API는 경로 열거 수준(딥한 취약점 진단 아님) — 정직히 밝힐 것 |
| 우대: MITRE ATT&CK 프레임워크 기반 보안진단 경험 | 위와 동일 | |

## 4. 실제로 있었던 일 (포트폴리오에 "증거"로 쓸 수 있는 사실들)

- **실서비스 운영 서버에서 실제 취약점 발견**: 사용자가 실제 운영 중인 서버 대상으로 파이프라인을 돌려, SSH 브루트포싱 3회 만에 **root 계정의 취약한 비밀번호로 실제 접속에 성공**. 실서비스 리스크 판단 후 사용자가 직접 중단(Ctrl+C, 정상 종료 확인). "그냥 스캐너 데모"가 아니라 실제로 뚫은 사례.
- **실제 VM(Ubuntu)에서 KISA CIIP 진단 67개 항목 중 66개 파싱 성공**, 실제 취약 항목(root 원격 접속 제한 미설정, `/etc/shadow` 권한 부적절, world-writable 파일 30개, crontab 권한 문제 등) 실측 발견.
- **IoT 대리 환경(OpenWrt VM)에서 발견한 환경 불일치**: ART 절차의 상당수가 `ash: xxx: not found`로 실패 — OpenWrt는 BusyBox 기반이라 GNU coreutils(`truncate`, `arp`, `systemctl`, `gcc` 등)가 없음. "이 절차가 이 환경에서 재현 가능한가"라는 진단 질문에 실패 자체가 답이 되는 사례로 문서화.
- **실전 디버깅으로 잡은 진짜 버그들**:
  - `subprocess.run`의 로케일 디코딩 오류(Windows cp949) → UTF-8 강제
  - Windows 백슬래시 경로가 `sh -c` 안에서 이스케이프로 오인식되는 문제
  - `sh`/`bash` PATH 탐색이 WSL 런처를 잘못 집는 문제 (Git for Windows 경로 우선 탐색으로 해결)
  - `subprocess.run(timeout=...)`이 손자 프로세스를 못 죽여서 파이프가 안 닫히고 무한 대기하는 문제 → `Popen`+데몬 리더 스레드+`taskkill /T`로 재작성
  - SFTP `mkdir`이 상위 디렉토리 자동 생성 안 하는 문제
  - `grep -ri password /`가 `/proc`의 가상 파일을 읽다가 멈추는 문제 → 위험한 기본 스캔 경로를 가진 후보 12개를 `$HOME /etc`로 제한
  - 서브넷 스윕 절차(`T1018`)가 실제 대상과 무관한 기본 서브넷(`192.168.1.0/24`)으로 254개 순차 ping을 돌아 사실상 못 쓰는 문제 → 안전 필터에서 제외
- **상태 기반 분기 로직 구현**: `requires`/`provides` 태깅 시스템으로, 조건 미충족 절차는 실행하지 않고 SKIP 처리, 성공한 절차의 결과(자격정보 등)를 실제로 상태에 반영해 이후 판단에 쓰이도록 구현. fixture 테스트로 자격정보 추출 정확성 검증.

## 5. 정직한 한계 (숨기지 말고 쓸 것 — 인터뷰에서 나올 질문들)

- **IoT 펌웨어/프로토콜 분석은 없음.** IoT 관련 포트(Modbus/MQTT/CoAP/TR-069 등)가 열려있는지 감지만 할 뿐, 실제로 그 프로토콜로 통신하거나 펌웨어 이미지를 분석(binwalk 등)한 적은 없음. 스코프를 "IoT모뎀↔내부망 구간"으로 의도적으로 한정했고, "센서↔모뎀 로컬 구간(Zigbee/BLE 등)"과 펌웨어 분석은 별도 전문 영역이라 제외했다고 설명할 것.
- **다중 호스트 확산(lateral movement)이 아직 없음.** 상태 기반 분기 메커니즘은 붙었지만, 지금은 대상 호스트 1개 안에서만 동작한다. 찾은 자격정보로 자동으로 다른 호스트에 연결을 시도하는 로직은 미구현 — 원래 기획서의 "접근 범위 확대" 서사를 완성하는 마지막 조각으로 다음 우선순위에 있음.
- **웹/API 딥 진단 없음.** 웹 경로 브루트포싱만 있고 실제 취약점(SQLi/XSS/인증 우회) 진단 로직은 없음.
- **CVE 매칭 재현율이 낮은 휴리스틱.** NVD 자유텍스트 검색이라 실제 버전 범위 매칭이 잘 안 됨(예: OpenSSH 8.9p1, nginx 1.18.0 등 최신 버전에서 0건). "CVE 0건 = 안전"이 아니라는 걸 명확히 알아야 함.
- **KISA CIIP은 Unix(Debian/RedHat 등) 카테고리만 연결됨.** Windows/웹서버/DBMS/PC(625개 중 나머지)는 원본 저장소에 있지만 아직 안 붙였음 — 실증 가능한 대상이 없어서 우선순위를 낮췄다고 설명할 것.
- **자동 테스트 코드가 없음.** 전부 수동 스모크 테스트로만 검증했음. 정직하게 인정할 부분.
- **cleanup_command가 실제로 VM에 흔적을 남겼다 지우는 방식이라, 일회용/스냅샷 VM이 아니면 위험.** 실서비스 서버 테스트 때도 이 리스크를 인지하고 사용자가 직접 판단해서 중단했음.
- **브루트포싱/ART 실행 코드가 그 자체로 공격 도구 성격을 가짐.** 공개 저장소에 올릴 때 "본인 소유 자산에만 사용" 같은 사용 범위 고지가 필요.
- **KISA-CIIP 통합 부분은 서드파티(AGPL-3.0) 코드를 원격 실행 가능하게 감싼 것.** breachchain이 "만든" 건 그 오케스트레이션 레이어(SFTP 업로드/결과 회수/리포트 통합)뿐이라는 걸 명확히 구분해서 설명할 것 — ART 원본 데이터도 동일한 원칙(직접 안 만들고 실행 라이브러리로 활용).

## 6. 기술 스택 / 아키텍처

- **언어**: Python 3.13
- **핵심 라이브러리**: `paramiko`(SSH), `PyYAML`(ART 정의 파싱), `mitreattack-python`(STIX 데이터셋 파싱)
- **데이터 소스**: 실제 Atomic Red Team 저장소(1786개 절차, sparse-checkout으로 필요한 부분만), MITRE ATT&CK STIX 데이터셋(697개 기법), KISA CIIP 2026 공개 진단 스크립트(rebugui/KISA-CIIP-2026)
- **아키텍처 (모듈 구성)**:
  - `art_loader.py` — 실제 ART YAML 스키마 파서 + 안전 필터(비파괴적/무권한상승/리눅스 대상만) + 위험 기본값 보정
  - `executor.py` — local/SSH 실행기, paramiko 기반, 프로세스 트리 강제 종료, 접속 사전 체크
  - `state.py` / `state_rules.py` — 상태 누적(자산/자격정보/접근) + requires/provides 기반 조건 판단
  - `tactic_mapping.py` — technique_id → ATT&CK tactic 매핑
  - `recon.py` / `bruteforce.py` / `web_recon.py` / `vuln_scan.py` — 초기 접근/정찰 단계
  - `device_fingerprint.py` — OS/커널/보드모델 기반 임베디드(IoT) 여부 자동 판정
  - `kisa_runner.py` — KISA CIIP 진단 스크립트 원격 실행 통합
  - `art_runner.py` — ART 후보 배치 실행 + 상태 기반 분기(`run_state_driven`)
  - `report.py` — 한국어 HTML 통합 리포트 렌더러
  - `pipeline.py` — 전체 흐름을 잇는 메인 진입점 (IP만 주면 끝까지 실행)
- **플로우차트**: `pipeline_flowchart_dean.drawio`(상세, 상태 기반 루프 내부 로직 포함), `pipeline_flowchart_summary_a4.drawio`(A4 가로 요약본) — 포트폴리오 첨부 이미지로 활용 가능

## 7. 실행 결과 수치 (있는 그대로)

- ART 전체 1786개 절차 파싱 → 안전 필터 통과 231개 (고유 기법 90개)
- ATT&CK 기법 697개에 대한 tactic 매핑 100% 성공 (미매핑 0개)
- 실제 VM(Ubuntu) KISA CIIP 진단: 67개 항목 중 66개 파싱 성공 (양호 46 / 취약 16 / 수동진단 3)
- 실서비스 서버: SSH 브루트포싱 3회 시도 만에 취약한 root 비밀번호 실제 발견
- 커밋 8개, 4일간 작업 (2026-08-12 ~ 2026-08-15)

## 8. 참고할 문서 (더 자세한 내용)

- `README.md` — 프로젝트 배경, 설계 결정과 근거(검토 후 제외한 대안 포함), 셋업/실행 방법, 전체 진행 체크리스트
- `reports.md` — 실전 테스트 중 발견한 인사이트/버그 상세 기록 (원인 분석 포함)
- `pipeline_flowchart_dean.drawio` — 상세 아키텍처 다이어그램
- 커밋 히스토리 — `git log`로 8개 커밋의 실제 변경 내용/타임라인 확인 가능

---

## 포트폴리오 작성 시 톤 가이드

- "다 만들었다"가 아니라 "이만큼 만들었고, 이건 다음 단계로 남겨뒀다"는 톤 유지
- 숫자(231개, 66/67, 3회 시도 등)는 그대로 인용해도 됨 — 전부 실제로 실행해서 나온 값
- 4일이라는 짧은 기간에 만들었다는 것 자체도 강점으로 쓸 수 있음 (단, "그래서 부족한 부분이 있다"는 맥락과 함께)
- KISA-CIIP/ART처럼 서드파티 데이터를 실행 라이브러리로 활용한 부분과, 직접 설계·구현한 오케스트레이션 레이어를 명확히 구분해서 서술 — 이게 안 되면 과장으로 비칠 위험이 있음
