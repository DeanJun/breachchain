attack_technique: T1021.004
display_name: Remote Services - SSH
description: >
  Establish an interactive-equivalent session on the internal node via SSH
  using harvested credentials, representing lateral movement / scope expansion.
platforms: [linux]
input_arguments: {}
executor:
  type: sh
  timeout: 15
  command: |
    hostname && uptime
